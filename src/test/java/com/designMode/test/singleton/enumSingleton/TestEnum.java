package com.designMode.test.singleton.enumSingleton;

import com.designMode.singleton.enumSingleton.Singleton;

public class TestEnum {
    public static void main(String[] args) {
        Singleton singleton = Singleton.singleton;
    }
}
